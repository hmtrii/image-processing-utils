from image_utils import *
from box_utils import *


if __name__ == '__main__':
    # image = cv2.imread("test-images/puppy.jpg")
    # boxes = np.array([
    # (12, 84, 640, 712),
	# (24, 84, 652, 712),
	# (36, 84, 664, 712),
	# (12, 96, 640, 724),
	# (24, 96, 652, 724),
    # (1000, 84, 1700, 1000),
	# (24, 108, 652, 736),
    # (1100, 50, 1800, 900)])

    # score = [1, 2, 3, 4, 5, 6, 7, 10]

    # nms_image = image.copy()
    # nms_boxes = nms(boxes, score)
    # plot_boxes(nms_image, nms_boxes, color=(0, 0, 255))
    # plot_boxes(image, boxes)
    # show_images([image, nms_image], 1, 2, save_file='test-output/test.png')
    print()